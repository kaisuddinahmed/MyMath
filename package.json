{
  "name": "mymath-workspace",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": ".venv/bin/python -m uvicorn backend.main:app --reload --host localhost --port 1233 --reload-dir backend --reload-exclude '.venv/*'",
    "dev:remotion": "node remotion/render-api.js",
    "dev:all": "bash ./scripts/dev-all.sh"
  }
}
